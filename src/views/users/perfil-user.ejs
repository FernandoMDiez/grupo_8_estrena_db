<!DOCTYPE html>
<html lang="en">

<!-- se obtiene el head de la carpeta Partials -->
<%- include("../partials/head-users") %>

  <body>
    <!-- se obtiene el header de la carpeta  Partials -->
    <%- include("../partials/header") %>
      <form method="POST" action="/users/user-profile">

        <div class="register-container">
          <h1>Listado de Perfiles</h1>
          <ul>
            <% for( let i=0; i < perfiles.length; i++ ) { %>
              <li>
                <a href="users/perfil-detail/<%= perfiles[i].idPerfil %>">
                  <%= perfiles[i].nameCategory %>
                </a>
              </li>
              <% } %>
          </ul>
          <div class="botones">
            <button class="addcart">
              <a href="" class="action-button edit">EDITAR PERFIL</a>
            </button>

            <div class="botones">
              <button class="addcart">
                <a href="" class="action-button edit">ELIMINAR PERFIL</a>

              </button>
            </div>
      </form>
  </body>

</html>